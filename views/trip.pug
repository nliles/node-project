extends layout

block content
  .inner#tripForm
    .single__hero
      #slide_cont
      br
      if trip.photo
        ul
          each photo in trip.photo
            li.tag
              img.mySlides(src=`/uploads/${photo}`)

      button.sliderButton.display-left ❮
      button.sliderButton.display-right ❯
      h2= trip.name
      p= trip.description


    //- .single__details.inner
    //-   img.single__map(src=h.staticMap(trip.locations.coordinates))
    //-   p.single__location= trip.locations.address
    
    #map Google Maps







